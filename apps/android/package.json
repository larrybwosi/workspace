{
  "name": "android",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "./gradlew assembleDebug",
    "lint": "echo skipping lint for android",
    "test": "echo skipping test for android"
  }
}
